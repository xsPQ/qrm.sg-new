*.log
.DS_Store
.env
.env.backup
.env.production

# Backup artifacts (never commit dumps/manifests/logs)
/backups/
/docker/backup/backups/
/docker/backup/logs/
*.dump
*.dump.gz
*.dump.gz.gpg
*.manifest.json
/restore-tests/

# Docker Build Quality Gate results/logs (OPS-2)
/build-results/
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# Frontend build output is committed (DEV-645): the local `php artisan serve`
# path used by smoke tests has no guaranteed `npm run build` step, and a
# gitignored public/build was repeatedly wiped on workspace resets, causing
# ViteManifestNotFoundException 500s. Docker still rebuilds fresh assets in the
# `frontend` stage (.dockerignore keeps host assets out of the image).
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
